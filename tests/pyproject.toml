[tool.poetry]
name = "tests"
version = "0.1.0"
description = "Unit and integration tests for OSR Console packages."
authors = ["Marsh Macy <mmacy@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^7.4.2"
osrlib = {path = "../osrlib"}
black = "^23.9.1"
flake8 = "^6.1.0"
flake8-docstrings = "^1.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
